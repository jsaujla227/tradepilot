import { Placeholder } from "../_components/placeholder";

export const metadata = { title: "Watchlist · TradePilot" };

export default function WatchlistPage() {
  return (
    <Placeholder
      title="Watchlist"
      description="Tickers to monitor with transparent scoring (trend, volatility, R-multiple, liquidity)."
      milestone="M8"
    />
  );
}
